---
title: Effortless AWS
description: Ship entire products on AWS serverless — API, database, website, queues, email — from TypeScript alone.
template: splash
---

import { Card, CardGrid, LinkButton, Tabs, TabItem } from '@astrojs/starlight/components';
import AnimatedHero from '../../components/AnimatedHero.astro';

<AnimatedHero />

<CardGrid>
  <Card title="Ship the whole product">
    API, database, queues, email, website — one project, one deploy, automatic IAM wiring.
  </Card>
  <Card title="Deploy in seconds">
    Direct AWS API calls. No CloudFormation, no state files. Full deploy in ~5-10 seconds.
  </Card>
  <Card title="Type safety everywhere">
    Define a table once — get typed clients, typed streams, and runtime validation automatically.
  </Card>
  <Card title="Serverless by default">
    Pay per use, scale to zero, multi-AZ redundancy. Nothing runs when nobody is using your product.
  </Card>
</CardGrid>

## Everything a product needs

| Your product needs | Effortless handler | AWS resources created |
|---|---|---|
| REST API | `defineApi` | Lambda + Function URL + IAM |
| Database | `defineTable` | DynamoDB + optional stream Lambda |
| Background jobs | `defineQueue` | SQS FIFO + consumer Lambda |
| File storage | `defineBucket` | S3 + optional event Lambda |
| Transactional email | `defineMailer` | SES + DKIM identity |
| Website / SSR app | `defineApp` | CloudFront + Lambda + S3 |
| Static site / SPA | `defineStaticSite` | CloudFront + S3 |

All in the same project, all deployed with one command, all with automatic IAM wiring between them.

<LinkButton href="/definitions/" variant="secondary" icon="right-arrow">All definitions</LinkButton>

## Build a todo app

Each tab adds a layer — every export creates real AWS infrastructure.

<Tabs>
  <TabItem label="todo.ts">
    Type and schema — shared across handlers.

    ```typescript
    // src/todo.ts
    import { z } from "zod";

    export type Todo = { id: string; title: string; done: boolean };

    export const Command = z.discriminatedUnion("action", [
      z.object({ action: z.literal("create"), title: z.string() }),
      z.object({ action: z.literal("done"), id: z.string(), done: z.boolean() }),
    ]);
    ```
  </TabItem>
  <TabItem label="HTTP API">
    One export — one API. Chained `.get()` routes handle queries, `.post()` handles commands.

    ```typescript
    // src/api.ts
    import { defineApi } from "effortless-aws";
    import { todos } from "./db";
    import { Command } from "./todo";

    export const todoApi = defineApi({ basePath: "/todos" })
      .deps(() => ({ todos }))
      .setup(({ deps }) => ({ todos: deps.todos }))
      .get({ path: "/" }, async ({ todos }) => ({
        status: 200,
        body: await todos.query({ pk: "TODO" }),
      }))
      .get({ path: "/{id}" }, async ({ req, todos }) => {
        const todo = await todos.get({ pk: "TODO", sk: req.params.id });
        if (!todo) return { status: 404, body: { error: "Not found" } };
        return { status: 200, body: todo.data };
      })
      .post({ path: "/", input: Command }, async ({ input, todos }) => {
        if (input.action === "create") {
          const id = crypto.randomUUID();
          await todos.put({
            pk: "TODO", sk: id,
            data: { tag: "todo", id, title: input.title, done: false },
          });
          return { status: 201, body: { id } };
        }
        await todos.update(
          { pk: "TODO", sk: input.id },
          { set: { done: input.done } },
        );
        return { status: 200, body: { id: input.id, done: input.done } };
      });
    ```
  </TabItem>
  <TabItem label="Database + stream">
    Define a table — get a typed client. `onRecord` fires a stream Lambda on every change. `config` reads secrets from SSM at cold start.

    ```typescript
    // src/db.ts
    import { defineTable } from "effortless-aws";
    import type { Todo } from "./todo";

    export const todos = defineTable<Todo & { tag: "todo" }>()
      .config(({ defineSecret }) => ({
        slackUrl: defineSecret({ key: "slack/webhook" }),
      }))
      .setup(({ config }) => ({ slackUrl: config.slackUrl }))
      .onRecord(async ({ record, slackUrl }) => {
        if (record.eventName === "MODIFY" && record.new?.data.done) {
          await fetch(slackUrl, {
            method: "POST",
            body: JSON.stringify({ text: `Done: ${record.new.data.title}` }),
          });
        }
      });
    ```
  </TabItem>
</Tabs>

### Deploy

```bash
$ eff deploy

Deployed 2 handler(s) in 6s:
  [table] todos    arn:aws:dynamodb:eu-west-1:***:table/todo-app-dev-todos
  [api]   todoApi  https://abc123.lambda-url.eu-west-1.on.aws/todos
```

Need a separate environment? Add `--stage`:

```bash
$ eff deploy --stage prod

Deployed 2 handler(s) in 6s:
  [table] todos    arn:aws:dynamodb:eu-west-1:***:table/todo-app-prod-todos
  [api]   todoApi  https://xyz789.lambda-url.eu-west-1.on.aws/todos
```

Fully isolated infrastructure — separate tables, Lambdas, Function URLs. No shared state between stages.

See how Effortless compares to SST, Nitric, Serverless Framework, and others — [detailed comparisons](/comparisons/).
