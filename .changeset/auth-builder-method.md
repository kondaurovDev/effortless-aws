---
"effortless-aws": minor
"@effortless-aws/cli": minor
---

- Add `.auth()` builder method to `defineApi` and `defineMcp` for configuring authentication
- Remove `enableAuth` from `.setup()` options in favor of the new `.auth()` method
